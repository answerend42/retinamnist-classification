_target_: src.models.retina_mnist_module.RetinaMNISTLitModule

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.001
  weight_decay: 0.0

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  _partial_: true
  mode: min
  factor: 0.1
  patience: 10

net:
  _target_: src.models.components.unet_classifier.UNetClassifier
  n_channels: 3
  n_classes: 5
  bilinear: false
  dropout_rate: 0.3

num_classes: 5

# compile model for faster training with pytorch 2.0
compile: false